# Comic App 开发计划

## 🎯 项目目标

开发一个桌面漫画阅读器，支持竖屏滚动阅读，能够从压缩包或文件夹加载图片。

---

## 🏗️ 架构设计

### 前端 (Vue 3 + TypeScript)

```
src/
├── components/
│   ├── FileTree.vue          # 文件树状图组件
│   ├── ComicViewer.vue       # 漫画竖屏阅读器组件
│   ├── ComicItem.vue         # 漫画项目（用于树状图）
│   ├── ThemeToggle.vue       # 主题切换组件
│   └── ProgressBar.vue       # 阅读进度条
├── views/
│   ├── HomeView.vue          # 主页/选择文件夹
│   └── ReaderView.vue        # 阅读页面
├── types/
│   └── index.ts              # TypeScript 类型定义
├── stores/
│   ├── comic.ts              # 漫画状态管理
│   ├── settings.ts           # 设置状态管理
│   └── bookmarks.ts          # 书签状态管理
├── utils/
│   └── storage.ts            # 本地存储工具
└── App.vue                   # 主入口
```

### 后端 (Rust/Tauri)

```
src-tauri/src/
├── lib.rs                    # Tauri 入口
├── file_system.rs            # 文件系统操作
├── zip_handler.rs            # ZIP 压缩包处理
├── image_handler.rs          # 图片处理
└── storage.rs                # 数据持久化
```

---

## 📦 功能模块

### 核心功能

| 模块         | 功能         | 说明                  |
| ------------ | ------------ | --------------------- |
| 文件系统扫描 | 扫描目录结构 | 识别 ZIP 和图片文件夹 |
| ZIP 处理     | 读取压缩包   | 提取图片列表和数据    |
| 文件树       | 树状图展示   | 展开/折叠/选择        |
| 漫画阅读器   | 竖屏滚动     | 懒加载、进度显示      |

### 扩展功能

| 模块      | 功能          | 说明                       |
| --------- | ------------- | -------------------------- |
| 阅读进度  | 记录/恢复位置 | 自动保存，下次打开跳转     |
| 书签/收藏 | 收藏管理      | 添加/删除/列表展示         |
| 缩放功能  | 多种显示模式  | 适应宽度、原始尺寸、自定义 |
| 分块加载  | 超长图处理    | 虚拟滚动、按需加载         |
| 主题切换  | 深色/浅色     | 系统跟随、手动切换         |

---

## 🔧 依赖清单

### Rust (Cargo.toml)

| 依赖                  | 版本   | 用途             |
| --------------------- | ------ | ---------------- |
| `zip`                 | latest | 读取 ZIP 压缩包  |
| `base64`              | latest | 图片编码传输     |
| `walkdir`             | latest | 递归遍历目录     |
| `natord`              | latest | 自然排序         |
| `tauri-plugin-dialog` | 2      | 文件夹选择对话框 |
| `tauri-plugin-fs`     | 2      | 文件系统访问权限 |
| `image`               | latest | 图片处理         |

### 前端 (package.json)

| 依赖                        | 用途               |
| --------------------------- | ------------------ |
| `@tauri-apps/plugin-dialog` | 调用文件选择对话框 |
| `@tauri-apps/plugin-fs`     | 文件系统操作       |
| `pinia`                     | 状态管理           |
| `vue-router`                | 路由管理           |
| `@vueuse/core`              | Vue 工具函数       |

---

## 📝 实现阶段

### 阶段 1: 基础设施

- [x] 创建计划文档
- [ ] 配置 Rust 依赖
- [ ] 配置前端依赖
- [ ] 配置 Tauri 权限

### 阶段 2: 后端核心

- [ ] 实现文件系统扫描 (file_system.rs)
- [ ] 实现 ZIP 处理 (zip_handler.rs)
- [ ] 实现图片处理 (image_handler.rs)
- [ ] 实现数据持久化 (storage.rs)

### 阶段 3: 前端核心

- [ ] 创建类型定义
- [ ] 实现状态管理 (Pinia stores)
- [ ] 实现文件树组件
- [ ] 实现漫画阅读器组件

### 阶段 4: 扩展功能

- [ ] 阅读进度记录与恢复
- [ ] 书签/收藏功能
- [ ] 缩放功能
- [ ] 大图分块加载
- [ ] 主题切换

### 阶段 5: 整合优化

- [ ] 整合所有模块
- [ ] 性能优化
- [ ] UI/UX 优化
- [ ] 测试与调试

---

## 🔄 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                        用户操作                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  HomeView: 选择文件夹                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Tauri Dialog → 用户选择根目录                        │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Rust 后端: 扫描目录                                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  file_system.rs                                      │    │
│  │  - 递归扫描目录                                       │    │
│  │  - 识别 ZIP 文件 → 标记为漫画                         │    │
│  │  - 识别图片文件夹 → 标记为漫画                        │    │
│  │  - 自然排序                                          │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  FileTree 组件: 显示树状结构                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  - 文件夹: 可展开/折叠                                │    │
│  │  - ZIP 文件: 显示为漫画                               │    │
│  │  - 图片文件夹: 显示为漫画                             │    │
│  │  - 点击选择漫画                                       │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Rust 后端: 加载漫画                                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  zip_handler.rs / image_handler.rs                   │    │
│  │  - ZIP: 解压获取图片列表                              │    │
│  │  - 文件夹: 读取图片列表                               │    │
│  │  - 按需加载图片数据 (Base64)                          │    │
│  │  - 大图分块处理                                       │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  ComicViewer 组件: 阅读漫画                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  - 竖屏滚动显示                                       │    │
│  │  - 懒加载图片                                         │    │
│  │  - 缩放控制                                          │    │
│  │  - 进度显示与保存                                     │    │
│  │  - 书签管理                                          │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 数据结构

### FileNode (文件树节点)

```typescript
interface FileNode {
  name: string;
  path: string;
  isDirectory: boolean;
  isComic: boolean; // 是否为漫画（ZIP 或图片文件夹）
  isZip: boolean;
  children?: FileNode[];
}
```

### ComicInfo (漫画信息)

```typescript
interface ComicInfo {
  path: string;
  name: string;
  isZip: boolean;
  imageCount: number;
  images: ImageInfo[];
}
```

### ImageInfo (图片信息)

```typescript
interface ImageInfo {
  index: number;
  name: string;
  path: string;
  width?: number;
  height?: number;
}
```

### ReadingProgress (阅读进度)

```typescript
interface ReadingProgress {
  comicPath: string;
  lastImageIndex: number;
  scrollPosition: number;
  lastReadTime: number;
}
```

### Bookmark (书签)

```typescript
interface Bookmark {
  id: string;
  comicPath: string;
  comicName: string;
  imageIndex: number;
  createdAt: number;
  note?: string;
}
```

### Settings (设置)

```typescript
interface Settings {
  theme: "light" | "dark" | "system";
  zoomMode: "fit-width" | "fit-height" | "original" | "custom";
  customZoom: number;
  preloadCount: number; // 预加载图片数量
}
```

---

## ✅ 完成标准

1. 能够选择文件夹并显示树状结构
2. 正确识别 ZIP 文件和图片文件夹为漫画
3. 竖屏滚动流畅阅读
4. 阅读进度自动保存和恢复
5. 书签功能正常工作
6. 缩放功能正常工作
7. 大图片分块加载无卡顿
8. 主题切换正常工作
9. 按名称自然排序正确
